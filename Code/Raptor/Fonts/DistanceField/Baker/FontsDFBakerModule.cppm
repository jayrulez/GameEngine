// Raptor::FontsDFBaker — the `raptor.fonts.df.baker` module.
//
// Import-time distance-field atlas baker (links msdfgen core).

export module raptor.fonts.df.baker;

export import :baker;
export import :init;
