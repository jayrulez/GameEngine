// Raptor::FontsDF — the `raptor.fonts.df` module.
//
// Runtime distance-field font atlas type: pure data, no msdfgen dependency.
// The import-time baker lives in a separate library (raptor.fonts.df.baker).

export module raptor.fonts.df;

export import :atlas;
