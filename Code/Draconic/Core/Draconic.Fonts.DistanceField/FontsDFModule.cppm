// Draconic::FontsDF - the `draconic.fonts.distancefield` module.
//
// Runtime distance-field font atlas type: pure data, no msdfgen dependency.
// The import-time baker lives in a separate library (draconic.fonts.distancefield.baker).

export module draconic.fonts.distancefield;

export import :atlas;
